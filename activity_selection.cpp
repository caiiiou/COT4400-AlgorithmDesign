#include <vector>
#include <utility>
using namespace std;

// TODO: Greedy activity selection
vector<int> activitySelection(vector<pair<int,int>>& activities) {
    vector<int> selected;
    return selected;
}
