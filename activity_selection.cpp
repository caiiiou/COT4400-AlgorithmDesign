#include <vector>
#include <utility>
using namespace std;

// TODO: Implement activity selection (greedy)
vector<int> activitySelection(vector<pair<int,int>>& activities) {
    vector<int> selected;
    return selected;
}
